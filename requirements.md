User Stories:
Users can see many movies layout on the website - MSlider X
Users can see paginate through all movies from the database - MGallery x, pagination
Users can see the detail of one single movie in a separate page, then select a movie from the list - DetailPage x
Users can see information about the movie (authors, description, title, genres …) x
Users can see filter UI and can filter movies by genres - category: genres list x, movie list by genre x
Users can see search UI and can search movies in the database by keyword - search by keyword x
Users can see the website is responsive for the minimum of Desktop, Phone, and Tablet - responsive
Users can see routes that allow user to revisit the same display when using such routes

API
https://developer.themoviedb.org/reference/intro/getting-started
